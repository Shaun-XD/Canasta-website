# URL of the FastAPI + Socket.IO backend (see server/README.md / docs/DEPLOY.md)
# Local:
VITE_SOCKET_URL=http://localhost:4000
# Vercel Production / Preview (set in the Vercel dashboard, not this file):
# VITE_SOCKET_URL=https://YOUR-API.up.railway.app
